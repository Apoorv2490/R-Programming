############### SIMULATING WHITE NOISE MODEL ARIMA(0,0,0)

y <- arima.sim(model = list(order = c(0,0,0)), n = 100, mean = 100, sd = 10) ## Creates a stationary time series data set

white_noise <- arima(y,order = c(0,0,0))

summary(white_noise_2)
